# Archive

Code kept for history. **Nothing here runs**, and nothing in `src/` imports it.

| folder | what it is | why it is kept |
|---|---|---|
| `i4_corridor_2026-08/` | the August 2026 I-4 corridor crash maps, 3D scenes and audits that came before the MEP project | the stacked-points, ramp-classification and false-hotspot lessons came from here |
| `retired_v1_steps/` | the first MEP version's steps 06–08 and 10–15 | two tests in `tests/` still guard mistakes these made; `12_full_run.py` is where the unsupported 93% figure came from (CORRECTIONS #55) |

`retired_v1_steps/10_screen_segments.py` contains a `TODO(human)` reserved for Yusra.
